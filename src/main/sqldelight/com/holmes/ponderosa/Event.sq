CREATE TABLE events (
  id INTEGER NOT NULL PRIMARY KEY,
  group_name TEXT NOT NULL,
  name TEXT NOT NULL
);

select_all:
SELECT *
FROM events;

insert_row:
INSERT OR REPLACE INTO events(id, group_name, name)
VALUES (?, ?, ?);

